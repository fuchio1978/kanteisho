# 四柱推命 命式作成サイト — Codex引き継ぎ

更新日: 2026-09-08

## 1. 作業対象と現在地

- メインリポジトリ: `C:\dev\kanteisho`
- GitHub: `fuchio1978/kanteisho`
- ブランチ: `main`
- この文書作成時のHEAD: `a7cc963`
- `main` と `origin/main` は同期済み。
- 本番アプリ: `https://kanteisho.onrender.com`
- 販売LP: `https://www.fuchilabo.com/meisiki`
- STORES: `https://fuchilabo.stores.jp/`
- 認証情報、APIキー、パスワード、管理者の個人情報は本書に記載していない。

## 2. 完了した主な実装

### 命式・鑑定機能

- 原命式八字、蔵干、五行変化、五行得点、強中弱、身旺判定、推命気温、用神候補、大運、年運を実装。
- 地方時差・均時差・日本のサマータイム補正を実装。
- 六柱推命モードを実装。
- 相性鑑定モードを実装。
  - 鑑定年は今年を中心に前後50年のプルダウン。
  - 最大4名を選択可能。
  - 表示順を任意に入れ替え可能。
  - 氏名をフルネーム表示。
  - 命式と五行サークルの重なり・距離を調整。
- PDF出力を実装・調整。
  - 通常鑑定書、六柱推命、相性鑑定に対応。
  - A4の余白、カード配置、改ページ、表示崩れを複数回修正。
  - 用途名は「講座生添削用」ではなく「鑑定士用」に変更済み。
- 生年月日のスマートフォン向け直接入力と元号入力を改善。

### 会員・料金プラン

- 確定プラン:
  - フリー: 月額0円。
  - スターター: 月額1,650円（税込）。命式保存なし。
  - プレミアム: 月額3,300円（税込）。相性鑑定・六柱推命・保存100件。
  - 講座生専用: 月額1,100円（税込）。全機能・保存無制限。
  - ご紹介用: 月額1,100円（税込）。全機能・保存無制限。
- 画面表示だけでなく、APIを直接呼んだ場合もプラン権限と保存上限を検証する。
- 会員本人の命式保存・検索・並べ替え・呼び出し・名称変更・削除を実装。
- 契約期限経過時は、アカウントを削除せずフリープランへ戻す。

### Supabase認証・データベース

- Supabase Authを使った個別会員ログインを実装。
- 無料会員の自己登録、確認メール、利用規約・プライバシーポリシー同意記録を実装。
- 招待メール、初期パスワード設定、パスワード再設定を実装。
- 招待済み・登録済みアカウントの復旧処理を実装。
- 管理者・会員・講座生共有版の認証経路を分離。
- 次のマイグレーションは本番Supabaseへ適用済み:
  - 会員基盤
  - サーバー権限分離
  - プランカタログ
  - STORES契約台帳
  - 会員招待
  - 無料登録と同意履歴
- 本番URLを、確認・招待・パスワード再設定の遷移先としてSupabaseに設定済み。
- 実メールで、確認、招待、再設定、変更後パスワードによるログインを確認済み。

### 管理者画面

- 会員のプラン・利用状態変更。
- 新規会員・共同管理者の招待。
- 登録済み無料会員への購入反映。
- STORES注文番号、購入メール、契約開始日、次回更新日、契約状態の手動記録。
- 継続決済確認後の「1か月更新」。月末補正と二重更新防止あり。
- 解約、期限切れ、返金済みの契約更新。
- 更新日が近い契約、支払確認中、期限経過のアラート。
- 契約・会員の検索と絞り込み、集計、操作履歴、契約CSV出力。
- 初回販売テストの手順を管理者画面へ追加。

### 販売LP・法務・LINE

- `meisiki.html` / `meisiki.css` に販売LPを実装。
- LPは「機能一覧」ではなく「鑑定士の仕事がどう変わるか」を主軸に構成。
- 相性鑑定、地方時差・均時差・サマータイム対応、運営者紹介を追記。
- 無料CTAは無料会員登録へ接続。
- LPの有料CTAはスターターとプレミアムだけが対象。講座生専用・ご紹介用はLPへ申込リンクを表示しない。
- 無料登録完了画面に公式LINEへの導線を追加。
- 公式LINE登録が任意であること、講座・イベント案内を配信する可能性、ブロック方法を表示。
- サイト登録メールを広告・宣伝の一斉配信に使う場合は別途同意を得る方針をプライバシーポリシーに記載。
- 利用規約、プライバシーポリシー、解約・返金ポリシー、特定商取引法ページを実装。
- 契約条件は以下で統一済み:
  - 月額自動更新。
  - 購入確認後、原則2営業日以内に有料プランを反映。
  - 次回注文作成の前日までにSTORESから解約。
  - 代行依頼は次回更新日の3営業日前まで。
  - 解約後も支払済み期間末まで利用可能。
  - 途中解約の日割り返金なし。
- 特商法ページには承認済みの運営者情報を反映済み。本書には個人情報を転記しない。

## 3. STORES側で完了した作業

- STORESは2026-09-08時点でスタンダードプランへアップグレード済み。
- 4商品はすべて1か月ごとの定期購入商品。
- 商品IDとサイト内プランの対応は `stores-catalog.js` に登録済み。
- 4商品とも在庫を「無制限」に変更済み。
- 4商品とも、利用開始、契約期間、自動更新、解約、返金、解約後データ、法務リンクを商品説明へ追記済み。
- 講座生専用・ご紹介用の商品説明冒頭へ対象者限定の警告を追加済み。
  - 対象確認ができない注文はキャンセル・返金。
  - ご紹介用はURLの第三者共有・転載禁止。
- 直近確認時点では4商品とも非公開。販売はまだ開始していない。

### STORESの重要な仕様制約と決定

- STORESの「アイテム限定公開」は定期便アイテムでは利用できない。スタンダードプランでも不可。
- 講座生専用・ご紹介用は定期便を維持する。
- 決定した運用:
  - LPにはスターター・プレミアムだけを掲載。
  - 講座生専用・ご紹介用の商品URLは対象者へ直接案内する。
  - ただし定期便商品を購入可能にするには通常公開が必要で、STORESのストア一覧から完全には隠せない。
  - 誤購入は商品名・冒頭警告・購入条件確認・対象外注文のキャンセルで抑止する。

## 4. STORES自動連携についての決定

- 販売開始時点ではSTORES注文APIによる自動連携を使わない。
- 購入・更新・解約・返金は管理者画面で手動反映する。
- 将来の自動連携を追加しやすいよう、以下は実装済み:
  - 商品IDとプランの一元対応。
  - 契約台帳。
  - 通知イベントの重複処理防止テーブル。
  - 契約状態から利用プランを決める共通ロジック。
  - 期限切れ・返金時のフリー復帰。
- 自動連携を行う場合は、STORES側の利用可能な注文API・Webhook、開発パートナー要件、認証方式を改めて確認する。

## 5. 別リポジトリ・公開LPに関する注意

- `https://www.fuchilabo.com/meisiki` の実体は、このリポジトリのRender URLとは別の自社サイト側から配信されている。
- 自社サイト側リポジトリは `fuchio1978/yoyaku-form`。
- 同リポジトリのコミット `16d80b0` で、サービス反映時期を「原則2営業日以内」に統一済み。
- 今後LP本文を変更するときは、`kanteisho/meisiki.html` だけでなく、実際に `www.fuchilabo.com/meisiki` を配信する自社サイト側の実装も確認すること。

## 6. 未完了作業

### 販売開始に必須

1. STORESでスターターだけを一時的に公開し、実決済を含む初回テスト購入を行う。
2. テスト購入者は、購入前に命式サイトの無料登録とメール確認を完了する。
3. 無料登録とSTORES購入に同じメールアドレスを使用する。
4. STORESで決済完了・注文番号・購入プラン・契約期間を確認する。
5. 管理者画面の「登録済み会員の購入を反映」から契約台帳とプランを手動更新する。
6. 購入者が再ログインし、スターター表示と未契約機能の制限を確認する。
7. STORESで解約テストを行い、管理者画面で「解約済み」と期間末を反映する。
8. 支払済み期間中の利用継続と、期間終了後のフリープラン復帰を確認する。
9. テスト成功後、残りの商品を公開する。
10. Renderでスターター・プレミアムの販売導線フラグだけをONにし、LPの「販売準備中」を正式なSTORESリンクへ切り替える。
11. 講座生専用・ご紹介用の販売導線フラグはOFFのまま維持し、URLを対象者へ個別送付する。

### 販売後または適切な時期

- 独自SMTPの設定。送信元アドレスは実施時にユーザーから改めて指定される予定。
- 会員・規約ページのドメインを `www.fuchilabo.com` 側へ統一。ユーザーから「適切なタイミングで」と指示あり。
- STORES注文API・Webhookによる自動契約反映。
- 必要に応じて、STORES上で対象者限定商品のストア一覧露出を減らせる新機能が提供されていないか再確認。

## 7. 次に実行すべき作業

次は「スターター商品の初回テスト販売」が本線。ただし商品公開と実決済は販売開始に直結するため、必ずユーザーの明示承認を得てから行う。

推奨順序:

1. STORESスターター商品を通常公開する。
2. LPの販売導線フラグはまだOFFのままにする。
3. ユーザーまたは指定テスト購入者が、無料会員登録済みのアドレスでスターターを実購入する。
4. 管理者画面で購入を手動反映する。
5. 権限、契約画面、保存不可、プレミアム機能不可を確認する。
6. 解約と契約終了処理を確認する。
7. 成功後にプレミアム、講座生専用、ご紹介用を公開する。
8. 最後にRenderのスターター・プレミアム販売導線だけをONにする。

## 8. 主要な変更ファイル

### アプリ・鑑定機能

- `app.js`: 命式計算、六柱推命、相性鑑定、PDF、入力改善。
- `index.html`: 鑑定画面、各モード、保存UI。
- `styles.css`: 通常画面、相性鑑定、PDF・印刷レイアウト。
- `use-god-data.js`: 用神データ。

### 会員・認証・契約

- `member-access.js`: プラン、権限、保存上限。
- `server.js`: ルーティング、会員画面、管理者画面、LP配信、法務ページ、LINE導線。
- `supabase-server.js`: Supabase Auth、プロフィール、保存命式、契約台帳、招待、再設定。
- `member-setup.js`: 招待後の初期パスワード設定。
- `member-password-reset.js`: パスワード再設定。
- `stores-catalog.js`: STORES商品ID・プラン対応・販売導線。
- `stores-subscription.js`: 契約状態の正規化と利用可否判定。

### LP・販売・設定

- `meisiki.html`: 販売LP。
- `meisiki.css`: 販売LPのレスポンシブデザイン。
- `STORES_POLICY_COPY.md`: STORES商品説明へ転記する共通文面。
- `render.yaml`: Render構成と本番用環境変数名。
- `.env.example`: ローカル設定例と販売導線フラグ。
- `README.md`: 構成、運用、販売開始前チェック。

### データベース

- `supabase/migrations/202608100001_member_foundation.sql`
- `supabase/migrations/202608110001_member_service_role_grants.sql`
- `supabase/migrations/202608110002_plan_catalog.sql`
- `supabase/migrations/202608160001_stores_subscriptions.sql`
- `supabase/migrations/202608170001_member_invitations.sql`
- `supabase/migrations/202608230001_public_free_signup_consents.sql`

### テスト

- `tests/regression.test.js`
- `tests/member-access.test.js`
- `tests/member-schema.test.js`
- `tests/server.test.js`
- `tests/stores-catalog.test.js`
- `tests/stores-subscription.test.js`
- `tests/supabase-server.test.js`

## 9. 検証状況

- 2026-08-31時点で `npm test` は202件すべて成功。
- 無料登録、確認メール、招待、パスワード再設定、各プラン表示を実ブラウザで確認済み。
- STORES商品説明、在庫無制限、非公開状態を管理画面で確認済み。
- 次の担当は作業前に、最新HEADで `npm test` と `git status -sb` を再実行すること。

## 10. 作業上の注意

- 現在の作業ツリーには、ユーザー所有の未追跡ファイル・ディレクトリがある。削除、移動、ステージングをしないこと。
- 特に `assets/`、`docs/`、`output2.csv`、既存の引き継ぎ文書はユーザー所有として扱う。
- コミットするときは対象ファイルを明示して `git add` する。
- 商品公開、LP販売リンクON、実購入、解約、返金、外部サービス設定変更は、実行直前にユーザー確認を取る。
- SupabaseやRenderの秘密値を画面・ログ・Gitへ出力しない。

## 11. 関連コミット

- `4c2077d` 相性鑑定モード初期版
- `e8984d0` 相性鑑定表示・並べ替え改善
- `b19ca6b` 相性鑑定PDF改善
- `b03e8a5` PDF印刷レイアウト修正
- `b277b8f` STORES商品対応とプラン制限検証
- `6e6d6c2` STORES契約台帳と利用判定
- `5df058f` 会員招待と初期パスワード設定
- `48b3a64` 購入者招待と契約記録の統合
- `95e9111` 管理者向け契約管理
- `1c95ea9` パスワード再設定
- `0685f87` 販売LP追加
- `a7ddfd9` 無料会員自己登録
- `f2bd713` 安全なSTORES販売リンク制御
- `80742bd` 契約・解約・返金条件確定
- `f5c59d1` LPへ法務・制作者情報を追加
- `3a163da` 公式LINE導線追加
- `d773477` LINE配信目的の明示
- `d749f25` 特商法の運営者情報反映
- `1307a21` 管理者画面へ手動販売テスト手順追加

