# 共同管理者アカウント招待・復旧対応 引き継ぎ

作成日: 2026-09-08  
対象リポジトリ: `C:\dev\kanteisho`  
対象ブランチ: `main`

## 目的

既存の管理者に加えて共同運営者用の管理者アカウントを追加し、本人がメール内のリンクからパスワードを設定して管理者として利用開始できるようにする。

本書には、対象者のメールアドレス、パスワード、認証トークン、Supabaseの接続情報、秘密鍵などの認証情報・秘密情報を記載していない。

## 実施済みの作業

### 1. 共同管理者専用の招待フローを追加

- 管理画面に「共同管理者を招待」フォームを追加した。
- 既存管理者だけが専用エンドポイントを実行できる。
- Supabase Authから本人へ招待メールを送る。
- 招待時のプロフィールは以下の状態にする。
  - `role = admin`
  - `plan_id = admin`
  - `account_status = invited`
- 本人がパスワード設定を完了すると `account_status = active` へ変更する。
- 通常会員の招待やプラン変更から管理者権限を付与することはできない。
- 管理者招待を `admin_invited` として監査記録へ保存する。

関連コミット: `4a5ff5e Add administrator invitation flow`

### 2. 一度だけのメールリンクを再試行可能に改善

初回の障害では、招待リンクを開いたあとに設定処理が失敗し、画面を閉じて同じメールを開き直すと「招待リンクを確認できませんでした」と表示された。

原因:

- Supabaseのメールリンクは初回クリック時に一回限りのコードが消費される。
- クライアントはURLから認証トークンをすぐ削除していた。
- パスワード更新とプロフィール有効化が別通信のため、途中失敗すると部分完了する可能性があった。

対応:

- 招待トークンとパスワード再設定トークンをブラウザの `sessionStorage` に一時保持する。
- 成功時だけ一時トークンを削除する。
- 同じタブの再読み込みや通信エラー後の再送信を可能にした。
- プロフィールがすでに `active` でも再実行を成功扱いにした。
- エラー表示で、画面を閉じずに再試行するよう案内する。

関連コミット: `63e806c Make invitation recovery retryable`

### 3. 登録済みアカウントの復旧経路を追加

最初の招待でAuthユーザーが作成済みの場合、再度「共同管理者を招待」を実行すると重複登録になる。

対応:

- 管理者招待で `already_registered` が返った場合、重複エラーで終了せず、同じアドレスへパスワード再設定メールを送る。
- パスワード再設定が成功した際、プロフィールが `invited` の場合だけ `active` へ変更する。
- `active`、`suspended`、`expired` の既存状態はこの復旧処理で変更しない。
- この経路は既存プロフィールの `role` や `plan_id` を変更しないため、通常会員を意図せず管理者へ昇格させない。

関連コミット: `434f89f Recover existing administrator invitations`

### 4. Supabase通信のタイムアウトを修正

パスワード設定時に約5秒で「処理が時間切れ」となる事象が発生した。

原因:

- パスワード更新とプロフィール有効化の2通信が同じ7秒タイマーを共有していた。
- 1回目の通信に時間を使うと、2回目に残された待ち時間が短くなっていた。

対応:

- `timedFetch` を追加した。
- パスワード更新とプロフィール有効化に、それぞれ独立した12秒のタイムアウトを設定した。
- 招待の初期パスワード設定とパスワード再設定の両方へ適用した。
- テストで2通信が別々のAbortSignalを使用することを確認している。

関連コミット: `f72d6f3 Give password reset steps independent timeouts`

## 決定事項

- パスワードは管理者が決めたり保存したりせず、本人だけがメール内の期限付きリンクから設定する。
- 管理者権限の付与は共同管理者専用の招待処理に限定する。
- 登録済みメールへの再招待は、権限変更ではなくパスワード再設定メールによる復旧へ切り替える。
- `invited` 状態だけをパスワード再設定完了時に `active` へ変更する。停止・期限切れ状態は維持する。
- URLに現れた認証トークンは履歴から除去し、再試行用には同一タブ限定の `sessionStorage` を使用する。
- 外部サービス間の完全なトランザクションは構成せず、冪等な再実行と復旧メールで途中失敗を回復する。

## 変更したファイル

### アプリケーション

- `server.js`
  - 管理画面の共同管理者招待フォーム
  - 管理者招待エンドポイント
  - 登録済みアカウントへの復旧メール切り替え
  - 成功・エラーメッセージ
- `supabase-server.js`
  - `inviteAdmin`
  - 管理者プロフィール作成
  - `completeMemberInvite` の冪等化
  - `resetMemberPassword` からの招待中プロフィール有効化
  - 通信ごとの `timedFetch` と独立12秒タイムアウト
- `member-setup.js`
  - 招待トークンの一時保持
  - 成功時の削除
  - 再試行可能なエラー案内
- `member-password-reset.js`
  - 再設定トークンの一時保持
  - 成功時の削除
  - 再試行可能なエラー案内

### テスト・文書

- `tests/server.test.js`
  - 共同管理者招待
  - 登録済み共同管理者への復旧メール
  - クライアント側のトークン一時保持
- `tests/supabase-server.test.js`
  - 管理者権限・監査記録
  - 招待完了と再設定完了
  - 招待中プロフィールの有効化
  - 通信タイムアウトの独立性
- `README.md`
  - 共同管理者招待、再試行、復旧メール、タイムアウト方針

データベースマイグレーションの追加はない。既存の `member_profiles` の `role`、`plan_id`、`account_status` を利用している。

## 検証結果

- 2026-09-08時点で `npm test` を実行済み。
- 全204テスト成功、失敗0。
- `main` のHEADは確認時点で `a7cc963`。
- `origin/main` も同じコミットを指しており同期済み。
- 上記4つの管理者招待関連コミットはすべて `main` の履歴に含まれている。
- 作業時には本番公開サービスのヘルスチェックが正常応答することを確認した。

## 未完了・未確認事項

- 最後のタイムアウト修正後、共同管理者本人が最新の再設定メールからパスワード設定を完了できたという最終報告は、この会話内ではまだ得られていない。
- 本番Supabase上の対象プロフィールが現在 `invited` か `active` かは、この環境からは確認していない。
- 本番Render/Supabaseの詳細ログには、この環境からアクセスしていない。
- メールアプリ内ブラウザ固有の挙動は実機での再確認が必要。

## 次に実行すべき作業

1. 現在の管理者で本番の会員管理画面を開く。
2. 「共同管理者を招待」へ共同管理者の名前と登録済みメールアドレスを入力して送信する。
3. 登録済みの場合、「復旧用のパスワード再設定メールを送信しました」という成功表示を確認する。
4. 共同管理者には、過去の招待メールではなく最新のパスワード再設定メールを使ってもらう。
5. メール内リンクを開いた画面で、新しいパスワードを2回入力し、ボタン押下後は最大25秒程度、画面を閉じずに待ってもらう。
6. 成功後、会員入口から本人のメールアドレスと設定したパスワードでログインする。
7. 管理画面へ遷移でき、共同管理者として会員情報を閲覧できることを確認する。

## 再発した場合の調査手順

本人から以下だけを取得する。パスワード、メール内URL全文、認証トークンは受け取らない。

- 画面に表示されたエラーメッセージ全文
- 発生日時（タイムゾーン付き）
- 使用端末とブラウザ／メールアプリ名
- ボタン押下からエラー表示までのおおよその秒数
- 同じ画面を閉じずに再度ボタンを押した結果

その時刻を基準に、デスクトップ側Codexまたは運用担当者が次を確認する。

1. Renderログで `/members/api/reset-password` または `/members/api/complete-invite` のHTTPステータスを確認する。
2. Supabase Authで対象ユーザーが存在することを確認する。
3. `member_profiles` で対象ユーザーの `role`、`plan_id`、`account_status` を確認する。
4. `role=admin`、`plan_id=admin`、`account_status=active` なら、パスワード設定処理は完了しているためログイン側を調査する。
5. `account_status=invited` のままなら、最新の再設定メールで再実行し、RenderからSupabase RESTへのプロフィール更新エラーを調査する。
6. HTTP 401ならリンク期限切れ／使用済み、400なら入力検証、503かタイムアウトなら上流通信またはプロフィール更新を重点的に調査する。

## Git作業上の注意

- ワークツリーには、この会話以前から存在する未追跡の `assets/`、`docs/`、`output2.csv` がある。
- これらは管理者招待対応へ含めず、削除・変更・コミットしていない。
- 今後のコミットでも、用途を確認せずにこれらをステージしないこと。

